{
  "name": "sbgamers-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create sbgamers-db",
    "db:migrate": "wrangler d1 execute sbgamers-db --file=../schema.sql",
    "db:migrate:local": "wrangler d1 execute sbgamers-db --local --file=../schema.sql"
  },
  "dependencies": {
    "itty-router": "^5.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241018.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
